language: python
python:
  - "3.6"
sudo: enabled
notifications:
  email: false
# command to install dependencies
install:
  - pip install -r requirements.txt
  - pip install pylint
  - pip install coveralls
# command to test
script:
  - ls -a
  - pylint Node/files
  - pylint Node/tests
  - coverage run --source=home pytest test home
after_success:
  - coveralls
